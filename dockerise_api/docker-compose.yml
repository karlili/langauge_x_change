version: '3.9'

services:
  api:
    build:
      context: ./
      dockerfile: ./Dockerfile

    restart: always

    volumes:
      - "./:/app"
      - "../whisper/training/model:/app/model"

    ports:
      - "8000:8000"

    command: >
      sh -c "uvicorn main:app --reload --host 0.0.0.0"
